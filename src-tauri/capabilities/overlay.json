{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "close-overlay",
  "description": "Minimal permissions for the floating auto-close warning panel. It only needs to listen for close events; Rust owns creating, positioning, showing and hiding the window, and its Cancel/Close now buttons call custom app commands, which are not ACL-gated.",
  "windows": ["close-overlay"],
  "permissions": ["core:default"]
}
